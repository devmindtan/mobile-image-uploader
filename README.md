# React Native MinIO Upload

Mobile app for selecting and uploading images to MinIO object storage.

## Tech

```bash
npx expo install expo-image-picker expo-file-system
npm install zustand nativewind
```